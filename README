This is Perl module CGI::Param::Filter.

INSTALLATION

CGI::Param::Filter installation is straightforward. If your CPAN shell is set up,
you should just be able to do

    % cpan CGI::Param::Filter

Download it, unpack it, then build it as per the usual:

    % perl Makefile.PL
    % make && make test

Then install it:

    % make install

DOCUMENTATION

CGI::Param::Filter documentation is available as in POD. So you can do:

    % perldoc CGI::Param::Filter

to read the documentation online with your favorite pager.

Yuji Shimada
